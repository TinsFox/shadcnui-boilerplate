# 数据请求

## 使用 `ofetch` 请求数据

[`ofetch`](https://github.com/unjs/ofetch)


## 使用 `@tanstack/react-query` 请求数据

[`@tanstack/react-query`](https://tanstack.com/query/latest/docs/framework/react/overview)

### 基本使用

### 使用 `useQuery` 请求数据

### 使用 `useMutation` 请求数据

### 使用 `useInfiniteQuery` 请求数据

### 使用 `useQueryClient` 请求数据
